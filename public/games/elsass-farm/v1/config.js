const Config = {
    debug: true,
    
    // Monde
    worldWidth: 3000,
    worldHeight: 3000,
    
    // Couleurs
    colors: {
        background: '#2c3e50',
        gridLines: 'rgba(255, 255, 255, 0.1)'
    }
};

window.ElsassFarm = {
    state: null
};